# ── ML / Data ─────────────────────────────────────────────────────────────────
lightgbm==4.3.0
scikit-learn==1.4.2
pandas==2.2.1
numpy==1.26.4
joblib==1.4.0

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
SQLAlchemy==2.0.29

# ── dbt ───────────────────────────────────────────────────────────────────────
dbt-postgres==1.8.0
dbt-core>=1.8.0,<2.0.0

# ── Airflow providers (installed alongside the official Airflow image) ─────────
# Note: apache-airflow-providers-google is already bundled in the base Airflow
# image and pins pandas<2.2. Do NOT add it here — it conflicts with pandas==2.2.1
# which is needed for the scorer and dbt feature pipeline outside Airflow.
apache-airflow-providers-postgres==5.10.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.31.0
click==8.1.7

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-cov==5.0.0
flake8==7.0.0
