[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --cov=src
    --cov-report=term-missing
    --cov-fail-under=55
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
